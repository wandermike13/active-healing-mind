{
  "type": "object",
  "properties": {
    "role": {
      "type": "string",
      "enum": [
        "user",
        "admin"
      ]
    },
    "is_premium": {
      "type": "boolean"
    },
    "push_token": {
      "type": "string",
      "description": "Expo push notification token registered on device"
    }
  },
  "required": []
}