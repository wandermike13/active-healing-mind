{
  "type": "object",
  "properties": {
    "role": {
      "type": "string",
      "description": "The role of the user in the app",
      "enum": [
        "admin",
        "user"
      ]
    },
    "is_premium": {
      "type": "boolean",
      "description": "Whether the user has purchased the premium upgrade"
    }
  },
  "required": [
    "role"
  ]
}